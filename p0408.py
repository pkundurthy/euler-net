
import numpy as num
import itertools as iter

def factorial(n):

	if n == 0:
		return 1
	else:
		return n*factorial(n-1)

def factorial_2(n):
	""" computes 2n! / n! """
	
	if x == n:
		return 1
	else:
		return factorial_2

def paths_square(n):

	for 

	2*n! / (n!)
